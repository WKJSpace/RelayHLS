# examples

Place small, shareable input examples here, such as detector-model snippets or compact JSON/CSV structures used with `generate_real_circuit_constants.py`.

Do not place large simulator dumps or generated HLS reports in this folder.
