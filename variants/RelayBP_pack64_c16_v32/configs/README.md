# configs

Configuration inputs for regenerating `src/constants.h`.

- `constants_config.json`: saved generator settings for this variant.

This directory should contain compact, human-reviewable configuration files only. Large simulation outputs and generated HLS reports should not be committed.
