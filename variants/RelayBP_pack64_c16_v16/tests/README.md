# tests

Python tests for the constant-generation tools.

Run from the variant directory:

```bash
python3 -m unittest discover -s tests
```

These tests check generator behavior and basic structural restrictions before using regenerated constants in HLS.
